{
  "name": "mcp-time",
  "version": "1.0.0",
  "description": "MCP server for getting current time",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc && chmod +x dist/server.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "time",
    "server"
  ],
  "author": "zepar",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "bin": {
    "mcp-time": "dist/server.js"
  }
}